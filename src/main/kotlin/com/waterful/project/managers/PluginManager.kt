package com.waterful.project.managers

/**
 * Plugin-level manager hub.
 * Career system managers are initialized directly in StarLightRe.onEnable()
 * via the career.manager package.
 */
object PluginManager {
    fun initialize() {
        // Additional plugin-wide initialization can go here
        // Career system initialization is handled in StarLightRe.kt
    }
}
